# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
motor==3.3.2  # Async MongoDB driver
pymongo==4.6.0
beanie==1.23.6  # MongoDB ODM for Python

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.1.2  # Required for passlib bcrypt backend (updated for Python 3.13 compatibility)
cryptography==41.0.7  # For credential encryption (Fernet)

# Redis (for caching and rate limiting)
redis==5.0.1

# AWS S3
boto3==1.34.10

# Email (Resend)
resend==0.8.0

# Validation & Serialization
pydantic>=2.9.0
pydantic-settings==2.1.0
email-validator==2.1.0
annotated-types==0.7.0

# HTTP Client
httptools==0.7.1  # For uvicorn performance

# Async Support
anyio==3.7.1
sniffio==1.3.1

# Web Sockets
websockets==15.0.1
python-socketio[asyncio]==5.16.0

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.3
dnspython==2.8.0  # For MongoDB srv connections
click==8.3.0

# Testing
# Test runner
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2  # For testing async endpoints
faker==20.1.0  # For generating test data

# AI Model Integrations
protobuf>=5.0.0  # Required for Python 3.14 compatibility
google-generativeai>=0.8.6  # Google Gemini (updated for Python 3.14 compatibility)
groq>=0.27.0  # Groq (LLaMA models) - updated for httpx compatibility

# Exchange Integrations
hyperliquid-python-sdk>=0.21.0  # Hyperliquid perpetual futures trading

# Development Tools
black==23.12.0  # Code formatting
flake8==6.1.0  # Linting
mypy==1.7.1  # Type checking
